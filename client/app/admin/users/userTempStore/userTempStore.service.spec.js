'use strict';

describe('Service: userTempStore', function () {

  // load the service's module
  beforeEach(module('affarisApp'));

  // instantiate service
  var userTempStore;
  beforeEach(inject(function (_userTempStore_) {
    userTempStore = _userTempStore_;
  }));

  it('should do something', function () {
  });

});
